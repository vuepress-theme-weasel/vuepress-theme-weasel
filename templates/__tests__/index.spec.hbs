{{!-- import { mount } from '@vue/test-utils'
import {{hump name}} from '@mr-huang/{{name}}'

describe('@mr-huang/{{name}}', () => {
  it('create', () => {
    const wrapper = mount(Sum{{hump name}})
    expect(wrapper.classes()).toContain('sum-{{name}}')
  })
}) --}}
